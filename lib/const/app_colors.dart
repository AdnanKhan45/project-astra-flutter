
import 'package:flutter/material.dart';

class AstraColors {

  static const Color primary = Color(0xFF2563EB);    // Bright Gemini blue
  static const Color secondary = Color(0xFF7C3AED);  // Vibrant violet
  static const Color tertiary = Color(0xFF60A5FA);   // Lighter sky blue

  // Backgrounds
  static const Color background = Color(0xFF0A0C16); // Deep space black/blue
  static const Color card = Color(0xFF16192A);       // Card dark surface

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF); // Pure white
  static const Color textSecondary = Color(0xFFB7C0DF); // Soft light blue-gray
  static const Color textMuted = Color(0xFF6B7280);   // Muted gray for hints

  // Accent & Actions
  static const Color accent = Color(0xFF00FFC6);     // Turquoise accent
  static const Color expBadge = Color(0xFF121A32);   // For [EXP] badge backgrounds

  // Dividers and borders
  static const Color divider = Color(0xFF232844);    // Subtle line
  static const Color border = Color(0xFF353B5A);     // Card/badge border
  static const Color greyBorder = Color(0xFF555058);     // Card/badge border

  // Success, warning, error
  static const Color success = Color(0xFF22D184);
  static const Color warning = Color(0xFFFBBF24);
  static const Color error = Color(0xFFEF4444);
  static const Color errorVibrantColor = Color(0xFFC3AFAC);
  static const Color errorDarkColor = Color(0xFF640710);

  // Overlay/Glow (use with opacity)
  static const Color glowBlue = Color(0xFF2563EB);
  static const Color glowViolet = Color(0xFF7C3AED);
}
